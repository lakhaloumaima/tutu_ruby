<h2><%= post.title %></h2>
<p><%= post.content %></p>
<p>
    <a href="<%= post_path(post.id) %>" class="btn btn-primary" > Lire la suite </a>
    <a href="<%= edit_post_path(post.id) %>" class="btn btn-default" > Editer </a>
    <a data-confirm="etes vous sur ?" data-method="DELETE"  href="<%= post_path(post.id) %>" class="btn btn-danger"  > Supprimer </a>
</p>